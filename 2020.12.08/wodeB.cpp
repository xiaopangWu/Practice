#include<stdio.h>

int main()
{
	int m;
	scanf("%d",&m);
	getchar();
	
	/*char str[200]={'\0'};
	int len=0;
	while((str[len]=getchar())!='\n')len++;
	for(int i=0;i<len;i++)
	{
		printf("%c",str[(len-m+i)%len]);
	}*/ 
	
	return 0;
}
